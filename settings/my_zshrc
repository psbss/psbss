# shell settings
## zsh auto complement
autoload -U compinit
compinit

## prompt initialize
autoload -U promptinit
promptinit

## use prompt pure
prompt pure

# alias
alias ll="ls -l"
alias la="ls -la"
alias rmf="rm -f"
alias rmr="rm -rf"

# PATH settings


